# Test wrong field name
service-definitions:
  database:
    type: database-factory

deployments:
  test:
    servers:
      api:
        base-url: http://localhost
        apps:
          - addr: ":3000"
            published-services: [database]
