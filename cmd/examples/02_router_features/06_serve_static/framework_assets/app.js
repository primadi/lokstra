// Framework default app.js
console.log("Framework default JavaScript loaded");

// This file is embedded in the binary and serves as a fallback
// when project-specific files don't exist
function frameworkInit() {
  console.log("Framework initialization complete");
}
